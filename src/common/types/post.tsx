export interface PostType {
  author: string;
  authorURL: string;
  title: string;
  date: string;
  slug: string;
  url: string;
  image?: string;
  context: string;
  content: string;
  excerpt: string;
}
